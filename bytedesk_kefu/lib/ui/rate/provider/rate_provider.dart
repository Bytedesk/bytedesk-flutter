import 'package:flutter/material.dart';

class RateProvider extends StatelessWidget {
  const RateProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
