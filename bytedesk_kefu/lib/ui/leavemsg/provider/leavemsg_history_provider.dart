import 'package:flutter/material.dart';

class LeaveMsgHistoryProvider extends StatelessWidget {
  const LeaveMsgHistoryProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        // child: child,
        );
  }
}
